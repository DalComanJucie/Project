package com.sp.unitt.files.domain;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class PersonTest {

    @Test
    public void GetName() //테스트가 초반에 안된다. 이유는 build grdle에서 junit에 대한 unitPratform을 선언 안해서 그럼
    {
        System.out.println("Test중입니다");
    }
}